rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 開発中は全てのアクセスを許可
    // ⚠️ 警告: この設定は開発環境のみで使用してください！
    // 本番環境では必ず適切なセキュリティルールを設定してください
    match /{document=**} {
      allow read, write: if true;
    }
  }
}