# Firebase Firestore インデックスの設定

## 必要なインデックス

アプリを正常に動作させるために、以下のFirestore複合インデックスを作成する必要があります：

### 1. pointTransactions コレクション

**フィールド:**
- `userId` (昇順)
- `createdAt` (降順)

**作成方法:**
1. Firebase Consoleにログイン
2. プロジェクト「ani-reco」を選択
3. Firestore Database → インデックス
4. 「インデックスを作成」をクリック
5. 以下の設定で作成：
   - コレクションID: `pointTransactions`
   - フィールド1: `userId` (昇順)
   - フィールド2: `createdAt` (降順)
   - クエリスコープ: コレクション

または、コンソールに表示されるリンクをクリックして直接作成できます。

## なぜ必要か

このインデックスは、ユーザーのポイント履歴を効率的に取得するために必要です。インデックスがないと、クエリがエラーになります。

## 確認方法

アプリを起動して、ポイント履歴が正常に表示されれば成功です。